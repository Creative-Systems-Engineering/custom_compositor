// App bar modules placeholders
pub mod dock {
    pub struct Dock;
}

pub mod launcher {
    pub struct AppLauncher;
}

pub mod widgets {
    pub struct SystemWidget;
}

pub mod config {
    pub struct AppBarConfig;
}
