// Styling and themes for glassmorphism/neomorphism
use glam::Vec4;

pub struct Style {
    pub background_color: Vec4,
    pub blur_radius: f32,
    pub opacity: f32,
}

pub struct Theme {
    pub name: String,
}
